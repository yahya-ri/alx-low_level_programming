#include "main.h"

/**
 * _strlen - get length of string.
 * @str: is a string.
 * Return: returns the length.
 */

int _strlen(char *str)
{
	size_t length = 0;

	while (str[siz] != '\0')
	{
		siz++;
	}

	return (siz);
}
